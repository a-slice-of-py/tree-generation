from setuptools import setup

setup(name='tree_generation',
      version='0.1',
      description='this is tree generation project setup template',
      author='Silvio Lugaro',
      author_email='silvio.lugaro@gruppoiren.it',
      packages=['tree_generation'], # this must have the same name as code folder (the actual package!)
      install_requires=['numpy']
      )